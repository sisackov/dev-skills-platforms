// king > gnome > elf > fire
// lemon > name > easy > yes

//"abc", "cdg", "gfe",
//"ghi", "ija"

// a > c > g > i, e > a

const suf = {
    g: [king],
    e: [gnome, fire, name],
    f: [elf],
    n: [lemon],
    y: [easy],
    s: [yes],
};

// const pref = { k, g, e, f, l, n, y };
